services:
  api:
    build: ./api
    ports:
      - "8000:8000"

  front:
    build: ./front
    ports:
      - "8080:8080"
    environment:
      API_URL: http://api:8000
    depends_on:
      - api
